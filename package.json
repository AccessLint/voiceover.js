{
  "name": "voiceover.js",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @accesslint/voiceover build",
    "test": "yarn workspace @accesslint/voiceover test"
  }
}
